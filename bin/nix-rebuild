#!/bin/bash

# --impure is required: several home-manager modules (vim, zsh, tmux,
# ghostty, alacritty configs) read dotfiles directly from this repo's
# absolute path at build time rather than copying them into the flake.
TMPDIR=/tmp sudo darwin-rebuild switch --show-trace --impure --flake "$HOME/src/me/dotfiles#lily"
